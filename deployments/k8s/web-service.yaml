apiVersion: v1
kind: Service
metadata:
  name: patient-timeline-web-service
  namespace: doolittle-health
  labels:
    app: patient-timeline-web
spec:
  selector:
    app: patient-timeline-web
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
